# ============================================
# The Unsent Daak - Spring Boot Configuration
# ============================================

server.port=8080

# ----- Database (Neon.tech) -----
spring.datasource.url=jdbc:postgresql://ep-withered-wave-a1wr10f7-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_4BIqw6leXmLc
spring.datasource.driver-class-name=org.postgresql.Driver

# ----- JPA / Hibernate -----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ----- JWT -----
jwt.secret=${JWT_SECRET:mySecretKeyForTheUnsentDaakApplicationThatIsLongEnough256bits}
jwt.expiration=86400000

# ----- Cloudinary -----
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:your_cloud_name}
cloudinary.api-key=${CLOUDINARY_API_KEY:your_api_key}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:your_api_secret}

# ----- CORS -----
frontend.origin=${FRONTEND_ORIGIN:http://localhost:3000}

# ----- File Upload -----
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ----- Actuator -----
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
